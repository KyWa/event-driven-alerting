---
apiVersion: triggers.tekton.dev/v1beta1
kind: Trigger
metadata:
  name: watchdog-example
spec:
  serviceAccountName: pipeline
  bindings:
  - kind: TriggerBinding
    ref: watchdog-example
  interceptors:
  - params:
    - name: "filter"
      value: "body.alerts.exists(a, a.labels.alertname == 'Watchdog')"
    ref:
      kind: ClusterInterceptor
      name: cel
  template:
    ref: watchdog-example
